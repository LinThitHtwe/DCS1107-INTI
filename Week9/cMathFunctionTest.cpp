#include <iostream>
#include <cmath>
using namespace std;

main(){
	int x,y = 3;
	float  result;
	
//	cout << "Enter first integer : ";
//	cin >> x;
	
	//to convert negative value to positive value
	//result = abs(x);
	
	// power - > base , raise of value
	//result = pow(x,y); // x raised the power of y x^y
	
	// round up to the nearest integer. parameter float
	//result = ceil(2.5);
	
	//round down to the nearest integer
	//result = floor(3.2);
	
	//squre root
	//result = sqrt(2);
	
	//modules of 2 real vaules
	//result = fmod(1,3);
	
	cout << "Result : "<< result << endl;
}